// variables4.rs
//
// Execute `rustlings hint variables4` or use the `hint` watch subcommand for a
// hint.


fn main() {
    let mut x = 3;
    println!("Number {}", x);
    x = 5; // don't change this line //*it reassignt the variable and it can only be done when variable is mutable 
    println!("Number {}", x);
}
